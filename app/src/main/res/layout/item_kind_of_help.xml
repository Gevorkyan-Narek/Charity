<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="168dp"
    android:layout_height="128dp"
    android:layout_marginBottom="@dimen/dp20"
    android:background="@color/light_gray">

    <ImageView
        android:id="@+id/image_kind_of_help"
        android:layout_width="@dimen/categoryIconWidth"
        android:layout_height="@dimen/categoryIconHeight"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/categoryIconMarginTop"
        android:src="@drawable/children"
        tools:ignore="ContentDescription" />

    <TextView
        style="@style/TitleCategory"
        android:id="@+id/name_kind_of_help"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/image_kind_of_help"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="@dimen/categoryTextMarginBottom"
        android:text="@string/children" />
</RelativeLayout>