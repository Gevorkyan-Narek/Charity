<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="55dp">

    <TextView
        android:id="@+id/filter_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginTop="@dimen/filterTextMargin"
        android:layout_marginEnd="@dimen/filterTextMargin"
        android:layout_toStartOf="@id/filter_switch"
        android:fontFamily="sans-serif"
        android:maxLines="1"
        android:singleLine="true"
        android:textColor="@color/black87"
        android:textSize="16sp"
        android:textStyle="normal" />

    <androidx.appcompat.widget.SwitchCompat
        android:id="@+id/filter_switch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="@dimen/filterSwitchButtonMargin"
        android:src="@drawable/chevron_right" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/filter_name"
        android:layout_marginTop="@dimen/filterTextMargin"
        android:background="@color/separator" />
</RelativeLayout>